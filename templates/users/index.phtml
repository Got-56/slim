<form action="/users" method="get">
    <input type="search" name="term" value="<?= htmlspecialchars($term ?? '') ?>" />
    <input type="submit" value="search">
</form>

<a href="<?= $router->urlFor('user.create') ?>">Create New User</a>

<ul>
    <?php if (empty($users)): ?>
        <li>No users found.</li>
    <?php else: ?>
        <?php foreach ($users as $user): ?>
            <li><?= htmlspecialchars($user['nickname']) ?> (<?= htmlspecialchars($user['email']) ?>, ID: <?= htmlspecialchars($user['id']) ?>)</li>
        <?php endforeach; ?>
    <?php endif; ?>
</ul>